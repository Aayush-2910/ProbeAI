/**
 * ThemeToggle — icon-only theme button.
 * Track A · FRONTEND-ARCHITECTURE.md §6, §8, §9
 *
 * Props: { isDark, onToggle }
 *
 * - Dark mode shows the SUN (click → light); light mode shows the MOON.
 * - Inline SVG only. No icon library. SVG is aria-hidden.
 * - 180deg rotation, 300ms, on toggle.
 * - No visible border; hover wash only.
 * - aria-label names the TARGET theme ("Switch to light mode").
 *
 * TODO(track-a): implement.
 */
